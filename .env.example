# TagStudio Configuration

# Path to your music library on the host (used by docker-compose)
MUSIC_DIR=/path/to/your/music

# Directory containing your music files inside the container
TAGSTUDIO_DATA_DIR=/data

# Web server settings
TAGSTUDIO_PORT=8080
TAGSTUDIO_HOST=0.0.0.0

# Authentication: first visitor creates admin account via web UI
# User data is stored in {TAGSTUDIO_DATA_DIR}/users.json

# Discogs API token (optional, for Discogs lookups)
# DISCOGS_API_TOKEN=your-token-here

# Frontend static files location (set automatically in Docker)
# TAGSTUDIO_STATIC_DIR=/srv/static
